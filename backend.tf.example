terraform {
  required_version = "~> 1.0"
  backend "s3" {
    bucket = ""
    key    = ""
    region = "ap-northeast-1"
  }
  required_providers {
    aws = {
      source = "hashicorp/aws"
    }
  }
}
